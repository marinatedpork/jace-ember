import { moduleFor, test } from 'ember-qunit';

moduleFor('service:points-channel', 'Unit | Service | points channel', {
  // needs: ['service:foo']
});

test('it exists', function(assert) {
  let service = this.subject();
  assert.ok(service);
});
