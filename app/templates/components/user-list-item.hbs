<div class="item-media">{{plus-one i}}</div>
<div class="item-inner">
  <div class="item-title">{{user.name}}</div>
  <div class="item-after">
    <span class="badge amber">{{user.numberOfBrosties}}</span>
    <span class="badge lightgreen">{{user.numberOfBadges}}</span>
    <span class="badge green">{{user.overallScore}}</span>
  </div>
</div>